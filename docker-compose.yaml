version: "3.8"

services:
  api:
    container_name: fidscript_api
    image: nextmavens/fidscript-whatsapp-api:latest
    restart: always
    depends_on:
      - redis
      - fidscript-postgres
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - fidscript_instances:/fidscript/instances
    networks:
      fidscript-net:
        aliases:
          - fidscript-whatsapp-api
          - fidscript_whatsapp_api
      dokploy-network:
        aliases:
          - fidscript-whatsapp-api
          - fidscript_whatsapp_api
    env_file:
      - .env
    expose:
      - "8080"

  frontend:
    container_name: fidscript_frontend
    image: nextmavens/fidscript-manager:latest
    restart: always
    # Port 3002 to avoid clash with fidscript-platform:3000
    ports:
      - "3002:80"
    networks:
      - fidscript-net

  redis:
    container_name: fidscript_redis
    image: redis:latest
    restart: always
    command: >
      redis-server --port 6379 --appendonly yes
    volumes:
      - fidscript_redis:/data
    networks:
      fidscript-net:
        aliases:
          - fidscript-redis
      dokploy-network:
        aliases:
          - fidscript-redis
    expose:
      - "6379"

  fidscript-postgres:
    container_name: fidscript_postgres
    image: postgres:15
    restart: always
    env_file:
      - .env
    command:
      - postgres
      - -c
      - max_connections=1000
      - -c
      - listen_addresses=*
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - fidscript-net
      - dokploy-network
    expose:
      - "5432"

volumes:
  fidscript_instances:
  fidscript_redis:
  postgres_data:

networks:
  # Shared with the Fidscript platform so the platform container can
  # reach this API via DNS name `fidscript-whatsapp-api` (or container
  # name `fidscript_api`).
  fidscript-net:
    name: fidscript-net
    external: true
  dokploy-network:
    name: dokploy-network
    driver: bridge
